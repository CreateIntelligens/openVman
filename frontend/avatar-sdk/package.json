{
  "name": "@openvman/avatar-sdk",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "test": "pnpm run build && node --test tests/*.test.mjs",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "pako": "^2.1.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "@types/pako": "^2.0.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
