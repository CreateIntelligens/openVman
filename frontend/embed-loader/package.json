{
  "name": "@openvman/embed-loader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "test": "npm run build && node --test tests/*.test.mjs",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.5"
  }
}
