# Community 124

Nodes: 6

## Members
- **slash_command.py** (`brain/api/core/slash_command.py`)
- **SlashRewrite** (`brain/api/core/slash_command.py`)
- **try_rewrite_slash()** (`brain/api/core/slash_command.py`)
- **Slash command resolver — rewrites /command messages into LLM-friendly instructio** (`brain/api/core/slash_command.py`)
- **A rewritten user message that instructs the LLM to call a specific tool.** (`brain/api/core/slash_command.py`)
- **If message starts with /skill_id, rewrite it to instruct the LLM to call the too** (`brain/api/core/slash_command.py`)

## Connections to other communities
