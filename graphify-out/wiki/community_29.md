# Community 29

Nodes: 28

## Members
- **workspace.py** (`brain/api/knowledge/workspace.py`)
- **get_workspace_root()** (`brain/api/knowledge/workspace.py`)
- **get_core_documents()** (`brain/api/knowledge/workspace.py`)
- **ensure_workspace_scaffold()** (`brain/api/knowledge/workspace.py`)
- **_migrate_learnings_layout()** (`brain/api/knowledge/workspace.py`)
- **get_archive_paths()** (`brain/api/knowledge/workspace.py`)
- **resolve_workspace_document()** (`brain/api/knowledge/workspace.py`)
- **resolve_workspace_artifact()** (`brain/api/knowledge/workspace.py`)
- **load_core_workspace_context()** (`brain/api/knowledge/workspace.py`)
- **is_indexable_document()** (`brain/api/knowledge/workspace.py`)
- **iter_workspace_documents()** (`brain/api/knowledge/workspace.py`)
- **iter_indexable_documents()** (`brain/api/knowledge/workspace.py`)
- **iter_knowledge_documents()** (`brain/api/knowledge/workspace.py`)
- **parse_identity()** (`brain/api/knowledge/workspace.py`)
- **Workspace paths, scaffold, and shared document helpers.** (`brain/api/knowledge/workspace.py`)
- **Return the workspace root for a given project.** (`brain/api/knowledge/workspace.py`)
- **Return the core document map for a given project.** (`brain/api/knowledge/workspace.py`)
- **Ensure the workspace root, core docs, and support directories exist.** (`brain/api/knowledge/workspace.py`)
- **Move LEARNINGS.md and ERRORS.md from workspace root into .learnings/.** (`brain/api/knowledge/workspace.py`)
- **Return archive directory paths for errors and memory.** (`brain/api/knowledge/workspace.py`)
- **Resolve a relative document path safely within the workspace.** (`brain/api/knowledge/workspace.py`)
- **Resolve a relative artifact path safely within the workspace.** (`brain/api/knowledge/workspace.py`)
- **Load the core markdown files used to steer chat generation.** (`brain/api/knowledge/workspace.py`)
- **Return whether a workspace file should be embedded into knowledge.** (`brain/api/knowledge/workspace.py`)
- **Return all text-like documents stored in the workspace.** (`brain/api/knowledge/workspace.py`)
- **Return workspace documents (including code files) for the knowledge index.** (`brain/api/knowledge/workspace.py`)
- **Return documents stored under the workspace knowledge/ directory.** (`brain/api/knowledge/workspace.py`)
- **Parse IDENTITY.md into structured fields (name, emoji, theme).** (`brain/api/knowledge/workspace.py`)

## Connections to other communities
